name: olaf-matchups
services:
  web:
    build: .
    ports:
      - "127.0.0.1:${OLAF_PORT:-8080}:8080"
    restart: unless-stopped
    read_only: true
    cap_drop: [ALL]
    security_opt:
      - no-new-privileges:true
